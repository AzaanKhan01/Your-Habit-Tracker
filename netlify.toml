[build]
  base    = "Frontend/ai-habit-tracker-ui-boilerplate-code"
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NPM_FLAGS = "--include=dev"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
